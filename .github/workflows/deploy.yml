name: deploy
on:
  push:
    branches:
      - main

jobs:
  gh-pages:
    runs-on: ubuntu-22.04
    steps:
      - name: Clone repository
        uses: actions/checkout@v3.1.0
      - name: Deploy documentation
        uses: ./.devcontainer
        with: { entrypoint: scripts/documentation/deploy.sh }
