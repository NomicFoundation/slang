---
"@nomicfoundation/slang": minor
---

Add `InterfaceDefinition::compute_interface_id`, computing the ERC-165 interface identifier as the XOR of the selectors of the interface's functions, including those inherited from base interfaces.
