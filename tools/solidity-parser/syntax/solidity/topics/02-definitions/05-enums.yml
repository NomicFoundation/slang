# yaml-language-server: $schema=../../../../../syntax-schema/syntax/schema/topic.schema.json

- name: EnumDefinition
  sequence:
    - terminal: enum
    - reference: Identifier
    - delimitedBy:
        open: "{"
        separatedBy:
          reference: Identifier
          separator: ","
        close: "}"
