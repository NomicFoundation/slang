use std::str::FromStr;
use crate::lexer::lexemes;

grammar<'source>(source: &'source str);

// pub Term: i32 = {
//     <n:Num> => n,
//     "(" <t:Term> ")" => t,
// };

pub Num: i32 = {
   <l:@L> <s:DecimalLiteral> <r:@R> => i32::from_str(source[dbg!(l)..dbg!(r)].trim()).unwrap(),
}

// pub PragmaDirective: String = {
//     PragmaKeyword_Reserved => {
//         let new_parser = Pragma::new();
//     }

// pub PragmaDirective: String = {
//     PragmaKeyword_Reserved_Switcher Slash <p: Pragma> Semicolon_Switcher => {
//         p
//     }
// }

// PragmaKeyword_Reserved_Switcher: () = { 
//     PragmaKeyword_Reserved => {
//         println!("Pragma keyword reducing");
//         lexer.switch_context(ContextKind::Pragma);
//         ()
//     }
// }

// Semicolon_Switcher: () = {
//     Semicolon => {
//         lexer.switch_context(ContextKind::Solidity);
//         ()
//     }
// }


// Pragma: String = {
//     <v:VersionPragma> => v,
//     // AbicoderPragma
//     // ExperimentalPragma
// }

// VersionPragma: String = {
//     <v:VersionExpressionSets> => v,
// }

// VersionExpressionSets: String = {
//     <mut v:(<VersionExpressionSet> BarBar)*> <e:VersionExpressionSet> => {
//         v.push(e);
//         v.join(" || ")
//     }
// }

// VersionExpressionSet: String = {
//     <v: (<VersionExpression>)*> => v.join(" && "),
// }

// VersionExpression: String = {
//     // VersionRange,
//     <v:VersionTerm> => v,
// }

// VersionTerm: String = {
//     <vOp: VersionOperator?> <v:VersionLiteral> => vOp.unwrap_or_default() + &v,
// }

// VersionOperator: String = {
//     Caret => String::from("^"),
//     Tilde => String::from("~"),
// }

// VersionLiteral: String = {
//     <v:SimpleVersionLiteral> => v,
//     // SingleQuotedVersionLiteral,
//     // DoubleQuotedVersionLiteral,
// }

// SimpleVersionLiteral: String = {
//     <mut v: (<VersionSpecifier> Period)*> <s:VersionSpecifier> => {
//         v.push(s);
//         v.join(".")
//     }
// }

// VersionSpecifier: String = {
//     <l:@L> VersionSpecifierLex <r:@R> => source[l..r].to_string(),
// }


extern {
    type Location = usize;
    type Error = ();

    enum lexemes::LexemeKind {
        {% for kind in model.parser.lexer.lexeme_kinds -%}
            {{ kind }} => lexemes::LexemeKind::{{ kind }},
        {% endfor %}
    }
}
