{
  "expected_solc_divergence": [
    {
      "specifier": {
        "type": "From",
        "from": "0.8.18"
      },
      "reason": "TODO(validation) SDR[1180]: from 0.8.18 the default EVM target is Paris, where `difficulty` is no longer an available built-in, so slang's availability-gated check stops firing and accepts the declaration. solc keeps rejecting the name as reserved (DeclarationError 5017)."
    }
  ]
}
