{
  "expected_solc_divergence": [
    {
      "specifier": {
        "type": "From",
        "from": "0.8.18"
      },
      "reason": "From '0.8.18' the default target is Paris or later, where solc emits `Warning 8417` that 'difficulty' was replaced by 'prevrandao'. Slang keeps `block.difficulty` available without a diagnostic, so it stays silent where solc warns."
    }
  ]
}
