{
  "override_language_versions": {
    "specifier": {
      "type": "From",
      "from": "0.8.1"
    },
    "reason": "solc 0.8.0 loops forever when assembly references a pure alias cycle of constants, so it cannot run these inputs at all. Every later version reports the cycle instead."
  }
}
