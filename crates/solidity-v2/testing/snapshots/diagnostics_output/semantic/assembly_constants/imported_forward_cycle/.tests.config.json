{
  "expected_solc_divergence": [
    {
      "specifier": {
        "type": "From",
        "from": "0.8.0"
      },
      "reason": "solc processes this import cycle with main.sol first, so the constant in lib.sol is not yet annotated and 2249 rejects the read. slang's forward reference test is same file only, so it accepts."
    }
  ]
}
