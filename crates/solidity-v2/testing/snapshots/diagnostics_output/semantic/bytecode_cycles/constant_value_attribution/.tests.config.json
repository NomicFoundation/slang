{
  "matrix": {
    "type": "SingleTargetAllVersions",
    "expected_solc_divergence": [
      {
        "specifier": {
          "type": "Till",
          "till": "0.8.4"
        },
        "reason": "Before '0.8.4' solc had no reachability analysis and only rejected a code access written inside the accessed contract, so it accepts this program. Slang implements the '0.8.4' call graph semantics for every version and reports the cycle on '0.8.0' to '0.8.3' as well."
      }
    ]
  }
}
