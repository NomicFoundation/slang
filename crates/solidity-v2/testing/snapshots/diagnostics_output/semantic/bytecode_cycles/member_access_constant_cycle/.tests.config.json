{
  "matrix": {
    "type": "SingleTargetAllVersions",
    "expected_solc_divergence": [
      {
        "specifier": {
          "type": "From",
          "from": "0.8.0"
        },
        "reason": "solc's reachability analysis does not follow constants referenced as members, so it never records A's dependency on itself and accepts this program on every version, then fails with an internal error during code generation. Slang follows the reference and reports the cycle on every version."
      }
    ]
  }
}
