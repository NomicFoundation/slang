// SPDX-License-Identifier: MIT
pragma solidity *;

// Slang only models ABI coder v2 (the default since 0.8.0), so `abicoder v1`
// is reported as unsupported. solc accepts it silently before 0.8.31 (and only
// warns from 0.8.31 on), hence the declared status mismatch.
pragma abicoder v1;
