// SPDX-License-Identifier: MIT
// A wildcard frees its own component without freeing the ones after it, so this
// admits only versions whose minor is `1` and whose patch is `0`.
pragma solidity x.1.0;
