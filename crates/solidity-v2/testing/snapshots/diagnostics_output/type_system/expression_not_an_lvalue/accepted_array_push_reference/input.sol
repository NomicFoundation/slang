// SPDX-License-Identifier: MIT
pragma solidity *;

// Accepted: `push()` appends an element and yields a reference to it.

contract Test {
  uint[] items;
  bytes data;

  function f() internal {
    items.push() = 1;
    data.push() = 0x01;
  }
}
