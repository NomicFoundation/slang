// SPDX-License-Identifier: MIT
pragma solidity *;

// A tuple denotes a value only if every element does.

function pick(bool c) pure returns (uint256, bool) {
    return c ? (uint, bool) : (uint, bool);
}
