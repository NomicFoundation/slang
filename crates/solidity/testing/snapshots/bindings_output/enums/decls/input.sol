contract Test {
    enum Answer { Yes, No }
}

library LibTest {
    enum Respuesta { Si, No }
}

interface Iface {
    enum Repondre { Oui, Non }
}

enum ActionChoices { GoLeft, GoRight, GoStraight, SitStill }
