contract Contract {
	function _transfer(address while) private {
		if(while == pair && !_isExcludedFromFee[to]){
			uint256 a = 1;
		}
	}

	function abc() {
		uint256 x = 0;
		unchecked { invalid sequence }
	}
}
