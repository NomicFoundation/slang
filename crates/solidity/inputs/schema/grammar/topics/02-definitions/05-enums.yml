# yaml-language-server: $schema=../../../../../../codegen/schema/generated/topic.schema.json

- name: "EnumDefinition"
  kind: "Parser"
  unversioned:
    sequence:
      - terminal: "enum"
      - reference: "Identifier"
      - delimitedBy:
          open: "{"
          expression:
            optional:
              separatedBy:
                separator: ","
                expression:
                  reference: "Identifier"
          close: "}"
