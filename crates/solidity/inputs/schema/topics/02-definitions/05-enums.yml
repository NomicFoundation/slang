# yaml-language-server: $schema=../../../../../codegen/schema/json-schema/topic.schema.json

- name: "EnumDefinition"
  sequence:
    - terminal: "enum"
    - reference: "Identifier"
    - delimitedBy:
        open: "{"
        separatedBy:
          reference: "Identifier"
          separator: ","
        close: "}"
