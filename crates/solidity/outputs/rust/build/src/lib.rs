// This is an empty crate. It only exists to have a `build.rs` that builds the public `slang_solidity` crate.
// This separation is done to avoid propagating our codegen crates and their dependencies to the public API.
